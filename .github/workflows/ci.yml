name: CI Build & Push

on:
  push:
    branches: [ "cicd" ]
  pull_request:
    branches: [ "cicd" ]

jobs:
  buildStep:
    uses: ./.github/workflows/build.yml
    with:
      environment: production

  testStep:
    uses: ./.github/workflows/test.yml
    with:
      environment: production

  packagingStep:
    needs: [buildStep, testStep]
    permissions:
      contents: read
      packages: write
      id-token: write
    uses: ./.github/workflows/packaging.yml
    with:
      environment: production


  # build-and-push:
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #     packages: write
  #     id-token: write

  #   steps:
  #     - name: Checkout repo
  #       uses: actions/checkout@v4

  #     - name: Log in to GitHub Container Registry
  #       uses: docker/login-action@v3
  #       with:
  #         registry: ghcr.io
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}

  #     # BACKEND
  #     - name: Build backend image
  #       run: docker build -t $REGISTRY/${IMAGE_NAME_BACK}:latest ./backend

  #     - name: Push backend image
  #       run: docker push $REGISTRY/${IMAGE_NAME_BACK}:latest

  #     # FRONTEND
  #     - name: Build frontend image
  #       run: docker build -t $REGISTRY/${IMAGE_NAME_FRONT}:latest ./frontend

  #     - name: Push frontend image
  #       run: docker push $REGISTRY/${IMAGE_NAME_FRONT}:latest

  #     # NGINX
  #     - name: Build nginx image
  #       run: docker build -t $REGISTRY/${IMAGE_NAME_NGINX}:latest ./nginx

  #     - name: Push nginx image
  #       run: docker push $REGISTRY/${IMAGE_NAME_NGINX}:latest
