name: Deploy

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu:24.04
    steps:
      - name: Deploy app
        run: echo "Deploying after build..."
