version: "3.9"

services:
  backend:
    image: ./back:latest
    ports:
      - "8080:8080"
    networks:
      - internal

  frontend:
    image: ./front:latest
    ports:
      - "3000:80"
    networks:
      - internal

  nginx:
    image: ./nginx:latest
    ports:
      - "80:80"
    depends_on:
      - backend
      - frontend
    networks:
      - internal

networks:
  internal:
    driver: bridge
